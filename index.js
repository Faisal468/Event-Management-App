import { registerRootComponent } from 'expo'; // Import the Expo function
import App from './App'; // Import the main App component

// Register your main App component as the root component of your app
registerRootComponent(App);
